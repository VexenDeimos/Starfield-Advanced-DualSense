#include <StarfieldDualSense/WwiseWindowsDeviceId.h>

// The implementation is constexpr/header-only so the same exact routine can be
// exercised by portable tests and used by the Starfield runtime diagnostic.
// Keep this translation unit so xmake exposes a dedicated source/target boundary
// for the v0.2.92 device-ID verification work.

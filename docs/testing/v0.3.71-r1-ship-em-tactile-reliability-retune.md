# v0.3.71-r1 EM Tactile Reliability Retune — Windows + Hardware Gate

Runtime marker: `0.3.71-ship-em-haptics-r1-tactile-reliability-retune`

Expected DLL versions:

- ProductVersion: `0.3.71.0`
- FileVersion: `0.3.71.0`

## Scope

This is a tactile-only retune. Exact EM authority `0x7A1A570C`, dynamic Wwise-object learning, the 180 ms Wwise-first correlation slot, same-event/same-object native held-fire ownership, 20 ms duplicate suppression, and release/menu/loading/pilot/shutdown clears are unchanged. There is no synthetic EM cadence.

Ballistic v0.3.63-r6, pulse laser v0.3.65-r2, Proton Beam v0.3.67, and missile v0.3.69 remain frozen.

## Retuned feel

Each confirmed native EM heartbeat emits one 75 ms `ShipEMPulse` at 0.70 base gain. The strengthened 126 Hz body, 338 Hz zap, and 515 Hz crackle produce about 0.140 actuator RMS at default strength, approximately the accepted laser crest's tactile level while remaining below Proton Beam. There is no continuous EM bed between shots.

R2 receives one finite 90 ms `EffectEx` pulse per confirmed EM heartbeat: start position 72, force envelope 132/196/112, frequency 92. It is intentionally easier to catch on quick single pulls while remaining lighter and higher-frequency than Proton Beam. The generic cockpit wall returns after the transient.

## Windows gate

Apply the r1 changed-files overlay over v0.3.71 and run the supplied Windows gate. Continue to hardware only if it ends with:

```text
V0.3.71-R1 EM TACTILE RELIABILITY RETUNE WINDOWS BUILD/INSTALL GATE PASSED.
Expected ProductVersion: 0.3.71.0
Expected FileVersion:    0.3.71.0
Expected runtime marker: 0.3.71-ship-em-haptics-r1-tactile-reliability-retune
```

## Hardware test

Use the same EM/Suppressor weapon on W0/R2. Fire 5-6 deliberate single pulls at varied pull speeds, then hold R2 for 3-4 native discharges. The body `bzz-ZAP` should now be clearly perceptible over propulsion. The trigger effect should catch quick single pulls reliably and still feel lighter/more electrical than Proton Beam.

Then open/close DataMenu and fire one more single shot to verify fresh reacquisition. Send the resulting `StarfieldDualSense(...).log` plus your tactile verdict.
